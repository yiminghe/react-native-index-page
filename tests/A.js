import React from 'react';
import {
  Text,
} from 'react-native';

const A = React.createClass({
  render() {
    return <Text>I am A</Text>;
  }
});

export const title = "A";

export const Demo = A;
